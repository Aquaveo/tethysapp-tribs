apiVersion: v1
kind: Service
metadata:
  name: {{ include "tribs.fullname" . }}
  labels:
    {{- include "tribs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tribs.selectorLabels" . | nindent 4 }}

