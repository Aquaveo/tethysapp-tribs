import PropTypes from "prop-types";

const GraphicsWindow = ({ datasets }) => <div>A mock with a prop passed!</div>;

GraphicsWindow.propTypes = {
  datasets: PropTypes.array,
};

export default GraphicsWindow;